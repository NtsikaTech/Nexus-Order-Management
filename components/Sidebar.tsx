import React from 'react';

// Sidebar is no longer used. Returning null to effectively disable it.
// This file can be deleted if no other part of the system might reference it for any reason.
const Sidebar: React.FC = () => {
  return null;
};

export default Sidebar;
